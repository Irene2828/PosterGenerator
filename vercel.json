{
  "version": 2,
  "framework": null,
  "buildCommand": "mkdir -p public && cp -r *.html fonts overrides.json public/ 2>/dev/null || true; cp -r *.png *.jpg *.jpeg *.pdf public/ 2>/dev/null || true",
  "outputDirectory": "public",
  "functions": {
    "api/*.js": {
      "includeFiles": "overrides.json"
    }
  }
}
