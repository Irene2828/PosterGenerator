{
  "name": "poster-generator",
  "version": "1.0.0",
  "description": "Terry Fox Run poster and flyer generator tool",
  "private": true,
  "scripts": {
    "build": "mkdir -p public && cp -r *.html *.png *.jpg *.pdf fonts overrides.json public/ 2>/dev/null || true",
    "dev": "node local-server.js"
  }
}
