{
  "buildCommand": "mkdir -p public && cp -r *.html *.png *.jpg *.pdf fonts overrides.json public/ 2>/dev/null || true",
  "outputDirectory": "public",
  "functions": {
    "api/*.js": {
      "includeFiles": "overrides.json"
    }
  }
}
